{
  "name": "slukta",
  "version": "1.0.0",
  "private": true,
  "description": "Slukta i bukta™",
  "scripts": {
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "typescript": "^6.0.2"
  },
  "packageManager": "pnpm@10.33.0"
}
